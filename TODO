o make ruby 1.9 compatible

o compatibility
  o with test/unit plugins
    x test/unit
    x minitest
    x context
    x shoulda
    x contest
    o test/spec
    ...
  o Rails

o focus a group of tests at once?

    focus do
      ...
      ...
    end
